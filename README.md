## 📘 PostgreSQL Database & SQL Practice Guide
---
## 🔍 Overview
<p>This repository contains hands-on PostgreSQL database practice, covering everything from database creation to advanced SQL concepts like joins, functions, views, triggers, and foreign keys.</p>

 ### It is designed for:
- Beginners learning SQL & PostgreSQL
- Backend developers practicing real-world queries
- Understanding relational database concepts clearly
- The project demonstrates clean SQL syntax, practical examples, and real-use cases commonly used in backend development.
  
##👉 Table Creation  PostgreSQL हमेशा eid को automatically generate करेगा। आप अपनी ID manually नहीं दे सकते।
```sql
CREATE TABLE contact (
  eid INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  name VARCHAR(50),
  email VARCHAR(50),
  mob VARCHAR(10),
  age INT,
  city VARCHAR(50)
);
```
## 👉 PostgreSQL automatically ID generate करेगा, लेकिन चाहें तो आप अपनी ID manually भी दे सकते हैं।
```sql
CREATE TABLE contact (
  eid INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name VARCHAR(50),
  email VARCHAR(50),
  mob VARCHAR(10),
  age INT,
  city VARCHAR(50)
);
```
### Insert Data
```sql
INSERT INTO contact(name,email,mob,age,city)
VALUES ('kumlesh','kumlesh7@gmail.com','1234567890',20,'Raipur');
```
### Select Queries
```sql
SELECT * FROM contact;
SELECT name, mob FROM contact;
SELECT name, mob, city FROM contact;
```
## 📌 WHERE Clause Examples
```sql
SELECT * FROM contact WHERE city='Raipur';
SELECT * FROM contact WHERE age < 19;
SELECT * FROM contact WHERE age > 19;
SELECT * FROM contact WHERE age = 19;
```
### AND / OR
```sql
SELECT * FROM contact WHERE age <= 19 AND city='Raipur';      Dono conditions true honi chahiye.
SELECT * FROM contact WHERE age <= 19 OR city='Raipur';       Koi bhi ek condition true ho to row aa jayegi.
```
### IN / NOT IN
```sql
SELECT * FROM contact WHERE city IN ('Mahasamund','Raipur');   Jinki city Mahasamund ya Raipur ho, unhe select karo.
SELECT * FROM contact WHERE city NOT IN ('Mahasamund','Raipur');  Jinki city Mahasamund aur Raipur ke alawa koi aur ho, unhe select karo.
```
## 📌 ORDER BY
```sql
SELECT * FROM contact ORDER BY name ASC;
SELECT * FROM contact ORDER BY name DESC;
```
## 📌 LIKE Pattern Matching
```sql
SELECT * FROM contact WHERE name LIKE 'k%';  name ka fist later k se start ho
SELECT * FROM contact WHERE name LIKE '_a%'; name ka second later a se start ho
SELECT * FROM contact WHERE name LIKE '%sh'; name ka last later sh se end ho
```
## 📌 Insert colum
```sql
ALTER TABLE contact
ADD COLUMN salary NUMERIC,
ADD COLUMN department VARCHAR(50);
```
```sql
ALTER TABLE contact
RENAME COLUMN mob TO mobile;

```
## किसी एक employee की salary और department update करना
```sql
UPDATE contact
SET salary = 25000,
    department = 'IT'
WHERE eid = 1;
```
## सभी records को एक ही value देना
```sql
UPDATE contact
SET salary = 20000,
    department = 'IT';
```
### Salary Queries {Aggregate Functions}
```sql
SELECT MAX(salary) FROM contact;  Highest Salary
SELECT MIN(salary) FROM contact;  Lowest Salary
SELECT AVG(salary::numeric) FROM contact;  Average Salary
SELECT SUM(salary::numeric) FROM contact;  Total Salary
SELECT COUNT(*) FROM contact;  Total Employees
SELECT name, salary FROM contact WHERE salary = (SELECT MAX(salary) FROM contact);  Highest Salary Employee
SELECT name, salary FROM contact WHERE salary = (SELECT MIN(salary) FROM contact);  Lowest Salary Employee

```
## 📌 LIMIT & OFFSET
```sql
SELECT name, salary FROM contact ORDER BY salary DESC LIMIT 3;  Top 3 Highest Salary Employee
SELECT name, salary FROM contact ORDER BY salary DESC LIMIT 1 OFFSET 1; Second Highest Salary employee
```
## 📌 Food Ordering Tables
### food_group
```sql
CREATE TABLE food_group (
  gid INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  group_name VARCHAR(50)
);
```
## Insert Data
```sql
INSERT INTO food_group(group_name)
VALUES ('Chinese');
```
## qtymast
```sql
CREATE TABLE qtymast (
  qid INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  qty_type VARCHAR(50),
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```
## Insert Data
```sql
INSERT INTO qtymast(qty_type)
VALUES ('full');
```
## Menu Data 
```sql
CREATE TABLE menu (
  mid INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  menu_name VARCHAR(50),
  menu_price INT,
  gid INT,
  qid INT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```
## Insert Data
```sql
INSERT INTO menu(menu_name, menu_price, gid, qid)
VALUES ('Momos','80','6','1');
```
## Foreign Key Constraint (Table Relationship)
```sql
ALTER TABLE menu
ADD CONSTRAINT fk_food_menu
FOREIGN KEY (gid)
REFERENCES food_group(gid);
```
```sql
ALTER TABLE menu
ADD CONSTRAINT fk_qty_menu
FOREIGN KEY (qid)
REFERENCES qtymast(qid);
```
## single Column data update
```sql
UPDATE menu
SET qid = 1
WHERE mid = 6;
```
## Column data update
```sql
UPDATE menu
SET qid = 2
WHERE mid IN (1,2,3,4,5,7,8,9,10);
```
## INNER JOIN सबसे ज़्यादा उपयोग होता है केवल वही records आएंगे जिनका gid दोनों tables में match करता है।
```sql
SELECT
    m.menu_name,
    m.menu_price,
    fg.group_name,
    q.qty_type
FROM menu m
INNER JOIN food_group fg
    ON m.gid = fg.gid
INNER JOIN qtymast q
    ON m.qid = q.qid;
```
## LEFT JOIN  menu table के सभी records आएंगे।
यदि food_group में match नहीं मिला तो group_name में NULL आएगा।
```SQL
SELECT menu_name, menu_price, group_name
FROM menu
LEFT JOIN food_group
ON menu.gid = food_group.gid;
```
## RIGHT JOIN food_group table के सभी records आएंगे।
यदि menu में match नहीं मिला तो menu_name और menu_price NULL होंगे।
```SQL
SELECT menu_name, menu_price, group_name
FROM menu
RIGHT JOIN food_group
ON menu.gid = food_group.gid;
```
## CROSS JOIN दोनों tables का हर record एक-दूसरे के साथ combine होगा।
यदि menu में 5 rows और food_group में 3 rows हैं, तो output में 15 rows आएंगी।
```SQL
SELECT menu_name, menu_price, group_name
FROM menu
CROSS JOIN food_group;
```
## UNION यह food_group table के group_name और qtymast table के qty_type को एक ही result में जोड़ देता है।
```sql
SELECT group_name FROM food_group
UNION
SELECT qty_type FROM qtymast;
```
## 📌 Calculations
```sql
SELECT menu_name,
       menu_price,
       2 AS qty,
       (2 * menu_price) AS total
FROM menu
WHERE mid = 2;
```
## 📌 String Functions
```sql
SELECT UPPER(menu_name) FROM menu;
SELECT REVERSE(UPPER(menu_name)) FROM menu;
SELECT LENGTH(menu_name) FROM menu;
SELECT REPLACE(menu_name,'ROL','ROLL') FROM menu;
```
## 📌 Math Functions
```sql
SELECT ABS(-10);
SELECT FLOOR(10.9);
SELECT CEIL(10.1);
SELECT ROUND(10.4);
```
## 📌 User Defined Function
```sql
CREATE OR REPLACE FUNCTION carea(r NUMERIC)
RETURNS NUMERIC AS $$
BEGIN
  RETURN 3.14 * r * r;
END;
$$ LANGUAGE plpgsql;
SELECT carea(8);
```
## मान लीजिए किसी company में हर product पर 18% GST निकालना है।
```sql
CREATE OR REPLACE FUNCTION gst(price NUMERIC)
RETURNS NUMERIC AS $$
BEGIN
    RETURN price * 0.18;
END;
$$ LANGUAGE plpgsql;

SELECT gst(1000);
```

step 1. cretate backup table 
```sql
CREATE TABLE back_contact (
  back_id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  emp_id INT,
  name VARCHAR(50),
  email VARCHAR(50),
  mobile VARCHAR(10),
  age INT,
  city VARCHAR(50),
  salary NUMERIC,
  department VARCHAR (50)
);
```

### Create a function Backup Before Delete
```sql
CREATE OR REPLACE FUNCTION backup()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO back_contact(emp_id,name, email, mobile, age, city, salary, department)
  VALUES (OLD.eid, OLD.name, OLD.email, OLD.mobile, OLD.age, OLD.city, OLD.salary, OLD.department);

  RETURN OLD;
END;
$$ LANGUAGE plpgsql;
```
## 📌 Create Triggers (Backup)
```sql
CREATE TRIGGER backup_trigger
BEFORE DELETE
ON contact
FOR EACH ROW
EXECUTE FUNCTION backup();
```
## View 
```sql
SELECT pg_get_functiondef('backup()'::regprocedure);
```
## Notes
Ab contact table me data delete karta hi back_contact table me jo data delete hua wo data automatic insert jayega 
```sql
DELETE FROM contact
WHERE eid = 8;
```
step 1. cretate audit table 
```sql
CREATE TABLE audit (
    audit_id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    emp_id INT,
    entry_date TIMESTAMP
);
```
### Create a function Audit After Insert
```sql
CREATE OR REPLACE FUNCTION auditlog()
RETURNS TRIGGER AS $$
BEGIN
INSERT INTO audit(emp_id, entry_date)
VALUES (NEW.eid, CURRENT_TIMESTAMP);
RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```
## Create Triggers (Backup & Audit)
```sql
CREATE TRIGGER audit_trigger
AFTER INSERT
ON contact
FOR EACH ROW
EXECUTE FUNCTION auditlog();
```
## Notes contact table me data insert karte hi automatic audit table me data insert ho jayega 
```sql
INSERT INTO contact(name,email,mobile,age,city,salary,department)
VALUES ('Rahul','rahul7@gmail.com','1234567890',24,'Raipur','35000','IT');
 ```
## Get the sequence name associated with the 'eid' identity column.
```sql
SELECT pg_get_serial_sequence('contact', 'eid');
```
## Restart the Auto identity sequence from 3.
```sql
ALTER SEQUENCE public.contact_id_seq RESTART WITH 3;
```
### Description:
Resets the PostgreSQL sequence so the next generated ID starts from 3.

## Add Column with Default Value
```sql
ALTER TABLE contact
ADD COLUMN status VARCHAR(20) DEFAULT 'active';
```
### Description:
Add a new Column status to the contact table with the default value set to 'active'.

## Reset Auto Increment Sequence

## 👨‍💻 Author

- Kumlesh Kurre
- Backend Developer
- Skills:  PostgreSQL
 
## ⭐ Support
If you like this project, please ⭐ star the repository to support my work!
  

